
import { IExitCheckerState, initialExitCheckerState } from "./exit-checker.state";
import { ExitCheckerActions, ExitCheckerActionTypes } from "./exit-checker.actions";

export function exitCheckerReducer(state: IExitCheckerState = initialExitCheckerState, action: ExitCheckerActions) {
  // switch(action.type) {

  //   default:
  //     return state;
  // }
}
