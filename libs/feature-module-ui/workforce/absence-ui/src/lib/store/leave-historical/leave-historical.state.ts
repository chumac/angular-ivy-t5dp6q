
export interface ILeaveHistoricalState {
  isProcessing: boolean;
  showEditor: boolean;
}

export const initialLeaveHistoricalState: ILeaveHistoricalState = {
  isProcessing: null,
  showEditor: null,
};
