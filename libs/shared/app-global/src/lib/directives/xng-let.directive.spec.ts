import { XngLetDirective } from './xng-let.directive';

describe('XngLetDirective', () => {
  it('should create an instance', () => {
    const directive = new XngLetDirective();
    expect(directive).toBeTruthy();
  });
});
